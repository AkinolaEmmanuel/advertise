/** Platform billing: merchants use pòlówó at no cost. */
export const PLATFORM_IS_FREE = true;

export const FREE_PLATFORM_FEATURES = [
  "Unlimited products",
  "WhatsApp & bank transfer checkout",
  "Custom storefront URL",
  "Order logs & analytics",
  "Theme customization",
] as const;
